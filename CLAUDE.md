# Ciel での作業ルール

このリポジトリは public です。更新の前に README の注意事項を読んでください。

## 応答の言語

- 日本語で書く。散文は日本語だけにする。
- 日本語以外の言語を混ぜない。韓国語・中国語の文字を出さない。
  意図せず混ざると、壊れているように見える。
- 英語の出だし（Now, Let me, First など）を使わない。
- 次のものは原文のままでよい。訳すとかえって使えなくなる。
  - コード、識別子、ファイル名、コマンド、設定のキー
  - ツールやサービスの名前、エラーメッセージの原文
  - 引用したドキュメントの原文

## 応答の書き方

- 「〜しています」と現在進行形で書く。
- アスタリスク2つでの強調を使わない。
- 前置きと言い直しを削る。結論から書く。

## ドキュメントとコメント

- 依頼のやり取りそのものは書かない。誰が読んでも分かる意図と設計だけを残す。
- なぜそうしたか（何を避けたかったか）を書く。何をしたかはコードを読めば分かる。

## 変更する前に

- スキルを足す・直したら `./scripts/validate.sh` を通す。
- 秘密情報が入っていないか `skills/secret-leak-check/scripts/scan.py --diff` で見る。
- 詳しい設計方針は [docs/GUIDELINES.md](docs/GUIDELINES.md)。

## 完了と言う前に

- 実際に動かす。書いただけで完了と言わない。
- コマンドの出力を捨てない。
- コミットしたら `git show --stat` で意図したファイルが入っているか見る。
- 確かめていないものは、確かめていないと書く。

## この設定の効く範囲

このファイルはコミットされるので、PC からでもスマホや Web の
セッションからでも効きます。`~/.claude/CLAUDE.md` に置いたものは
その機械の中だけなので、リモートのセッションには引き継がれません。
どこからでも効かせたいルールは、このファイルに書いてください。
